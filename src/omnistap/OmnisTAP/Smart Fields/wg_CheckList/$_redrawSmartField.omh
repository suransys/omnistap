# This method is called to redraw the contents of the smartfield. The super class will call this on $setExternalValue.
# Disabling this method, otherwise clicking a checkbox directly re-scrolls the list to the last selected line
If mouseover(kMLine)=_ilList.$line
	Do inherited
End If