# Enter your initialization code here
Do inherited

Do ioTypeToSearch.$disableListReselectionOnChange()

If not(isclear(pcCalculation))
	Do $cinst.$setCalculation(pcCalculation)
End If

If not(isclear(pcTypeToSearchCalculation))
	Do $cinst.$setTypeToSearchCalculation(pcTypeToSearchCalculation)
End If

Do $cinst.$initCheckList()