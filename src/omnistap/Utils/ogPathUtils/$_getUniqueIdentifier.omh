If tvMacVersion
	Quit method uoProcess.$getMyProcessID()
End If

;  add in a #CT, just for randomness' sake
Calculate lcCT as abs(#CT)
If len(lcCT)>10
	Calculate lcCT as right(abs(#CT),10)
Else
	Calculate lcCT as jst(lcCT,'-10P0')
End If

Quit method lcCT